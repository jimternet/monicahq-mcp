# MonicaHQ API Configuration
# Copy this file to .env and fill in your actual values

# MonicaHQ Instance URL
# Example: https://app.monicahq.com/api or https://your-instance.monicahq.com/api
MONICA_API_URL=https://app.monicahq.com/api

# MonicaHQ OAuth2 Bearer Token
# Generate from MonicaHQ Settings → API → Personal Access Tokens
# Format: Bearer token (without the "Bearer" prefix - just the token)
MONICA_API_TOKEN=your-oauth2-bearer-token-here

# Optional: Override default port (default: 8080)
# SERVER_PORT=8080

# Optional: Set environment (development, production)
# SPRING_PROFILES_ACTIVE=development

# Optional: Override WebSocket configuration
# MCP_WEBSOCKET_PATH=/mcp
# MCP_WEBSOCKET_MAX_SESSIONS=10

# Optional: Logging level (DEBUG, INFO, WARN, ERROR)
# LOGGING_LEVEL_COM_MONICAHQ_MCP=DEBUG