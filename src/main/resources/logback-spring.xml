<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- Define correlation ID pattern -->
    <property name="CORRELATION_ID_LOG_PATTERN" value="[%X{correlationId:-NO_CORRELATION_ID}]"/>
    
    <!-- Console appender with structured logging - MUST use stderr for MCP STDIO protocol -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{ISO8601} ${CORRELATION_ID_LOG_PATTERN} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File appender for application logs -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/monicahq-mcp.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/monicahq-mcp-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{ISO8601} ${CORRELATION_ID_LOG_PATTERN} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Async appender for better performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>
    
    <!-- JSON structured logging for production - MUST use stderr for MCP STDIO protocol -->
    <springProfile name="production">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <target>System.err</target>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"service":"monicahq-mcp","version":"0.1.0"}</customFields>
                <includeMdc>true</includeMdc>
                <includeContext>true</includeContext>
                <includeCallerData>true</includeCallerData>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Standard logging for development -->
    <springProfile name="!production">
        <!-- Application loggers -->
        <logger name="com.monicahq.mcp" level="DEBUG"/>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="org.springframework.websocket" level="DEBUG"/>
        <logger name="io.github.resilience4j" level="INFO"/>
        
        <!-- Root logger -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Specific logger for MCP protocol messages -->
    <logger name="com.monicahq.mcp.protocol" level="TRACE" additivity="false">
        <appender-ref ref="FILE"/>
    </logger>
    
    <!-- Logger for MonicaHQ API client -->
    <logger name="com.monicahq.mcp.client" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
</configuration>